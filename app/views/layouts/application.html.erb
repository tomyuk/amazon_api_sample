<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="X-UA-Compatible" content="IE=10">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no">
    <title><%= content_for?(:title) ? yield(:title) : "EMS Print" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "EmsPrint" %>">
    <meta name="author" content="<%= content_for?(:author) ? yield(:author) : "EmsPrint" %>">
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">

    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/">EMS発送ラベル自動印刷</a>
        </div>
      </div>
      <% if user_signed_in? %>
        <div class="collapse navbar-collapse" id="emsprint-navbar-collapse">
          <ul class="nav navbar-nav">
            <li <%== active_menu("home") %>><a href="/">ダッシュボード</a></li>
            <li <%== active_menu("invoices") %>><a href="<%= new_invoice_path %>">送り状作成</a></li>
            <li <%== active_menu("system_logs") %>><a href="<%= order_history_path %>">送信履歴</a></li>
            <li <%== active_menu("settings") %>><a href="<%= edit_setting_path(1) %>">設定</a></li>
            <li><a href="<%= destroy_user_session_path %>">ログアウト</a></li>
          </ul>
        </div><!-- /.navbar-collapse -->
      <% end %>
    </nav>
    <div class="container-fluid" id="main">
      <% if user_signed_in? %>
        <%= render 'layouts/default' %>
      <% else %>
        <%= render 'layouts/login' %>
      <% end %>
    </div>
  </body>
</html>
