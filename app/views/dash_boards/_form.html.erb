<%= form_for(@dash_board) do |f| %>
  <% if @dash_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dash_board.errors.count, "error") %> prohibited this dash_board from being saved:</h2>

      <ul>
      <% @dash_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
